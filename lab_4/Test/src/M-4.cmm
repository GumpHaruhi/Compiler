// 结构体作为变量
struct Operands{
    int o1;
    int o2;
};

int addfunc(struct Operands temp){
    return temp.o1 + temp.o2;
}

int main(){
    int n;
    struct Operands op;
    op.o1 = read();
    op.o2 = read();
    n = addfunc(op);
    write(n);
    return 0;
}